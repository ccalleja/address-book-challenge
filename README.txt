Steps to run the project

1. Compile it using maven command "mvn clean install"
2. Run the jar file generated in the target folder named "address-book-1.0.RELEASE.jar"


NOTE : You can import the project in an IDE via maven

I can be contacted on clint.calleja@gmail.com
